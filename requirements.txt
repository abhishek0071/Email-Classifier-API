# ─── runtime deps ───────────────────────────────────────────────
fastapi==0.114.1          # API framework
uvicorn[standard]==0.29.0 # ASGI server (+uvloop, httptools, watchfiles)

pydantic==2.11.7          # core validation (≥ 2.7.3 to satisfy mcp)
pydantic-settings==2.10.1 # new import path for BaseSettings

requests==2.32.3          # sync HTTP client
python-dotenv==1.0.1      # optional .env loading
